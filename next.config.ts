console.log(">>> DEBUG: NEXTAUTH_URL em next.config.ts:", process.env.NEXTAUTH_URL);
console.log(">>> DEBUG: Todas as envs em next.config.ts:", JSON.stringify(process.env));
import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  /* config options here */
};

export default nextConfig;
